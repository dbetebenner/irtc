# Large PDFs in the document bundle are LFS-tracked; thesis.pdf (rebuilt
# on every push, can balloon LFS quota) stays regular.
context/**/*.pdf filter=lfs diff=lfs merge=lfs -text
ui/www/docs/thesis.pdf -filter -diff -merge
