% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heldout.R
\name{heldout_logloss}
\alias{heldout_logloss}
\title{Held-out marginal log-loss at fixed parameters}
\usage{
heldout_logloss(result, newdata, nq = 61)
}
\arguments{
\item{result}{An \code{irtc_model_result} from \code{\link[=fit_baseline]{fit_baseline()}},
\code{\link[=fit_irtc]{fit_irtc()}}, or \code{\link[=fit_copula_1f]{fit_copula_1f()}}.}

\item{newdata}{A response matrix (or \code{irtc_simulation}) with the same
items, in the same order and coding, as the training data.}

\item{nq}{Quadrature points (default 61; use the fitting \code{nq} to
reproduce a training likelihood exactly).}
}
\value{
A list of class \code{irtc_heldout}: \code{total_loglik}, \code{n_persons},
\code{n_responses}, and \code{logloss_per_response} (negative mean).
}
\description{
The M4 primary metric: the marginal log-likelihood of unseen persons'
response vectors under a fitted model's (training-time) parameters, with
the latent variable integrated out by quadrature, reported per response.
}
\details{
Two paths, one per model class:
\itemize{
\item Binary marginal-IRT results (engines \code{mirt} -- models \verb{2pl},
\code{rasch} -- and \code{irtc}): standard-normal ability prior, item response
\code{plogis(a (theta - b))}.
\item One-factor copula results (engine \code{FactorCopula}): uniform latent
factor, category probabilities reconstructed from the stored
first-stage cutpoints and per-item copula h-functions
(\code{bvn}, \code{frk}, \code{gum}, \code{rgum}, \code{joe}, \code{rjoe}).
}

Correctness contract (enforced in tests): evaluated on the training data
at the fitted parameters, the result reproduces the engine's own
log-likelihood. Missing responses contribute nothing (skipped, never
imputed). GRM results are not yet supported.
}
