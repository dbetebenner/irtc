% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_grm}
\alias{simulate_grm}
\title{Simulate ordinal responses from a graded response model (GRM)}
\usage{
simulate_grm(n_persons, n_items, n_categories, seed)
}
\arguments{
\item{n_persons}{Number of persons (rows). Must be at least 1.}

\item{n_items}{Number of items (columns). Must be at least 2.}

\item{n_categories}{Number of ordered response categories (at least 2).
Responses take values in \code{1:n_categories}.}

\item{seed}{Integer seed. Required -- unseeded simulations are not
reproducible and the charter forbids them in registered experiments.}
}
\value{
An \code{irtc_simulation}; \code{item_parameters} holds \code{item}, \code{a}, and
threshold columns \verb{b1 ... b\{n_categories - 1\}}.
}
\description{
Person abilities and discriminations as in \code{\link[=simulate_2pl]{simulate_2pl()}}. Each item
gets \code{n_categories - 1} strictly increasing thresholds (a uniform start
plus positive uniform gaps). Responses follow Samejima's graded response
model: \verb{P(Y >= k | theta) = plogis(a * (theta - b_\{k-1\}))}.
}
\seealso{
Other simulation:
\code{\link[=simulate_2pl]{simulate_2pl()}}
}
\concept{simulation}
