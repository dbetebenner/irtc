% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-result.R
\name{new_model_result}
\alias{new_model_result}
\title{Construct a standardized model-fit result}
\usage{
new_model_result(
  model,
  engine,
  engine_version,
  data_description,
  seed,
  converged,
  log_likelihood,
  n_parameters,
  estimates,
  warnings = character(0),
  runtime_seconds
)
}
\arguments{
\item{model}{Model identifier (e.g. \code{"rasch"}, \code{"2pl"}, \code{"grm"}).}

\item{engine}{Fitting engine (e.g. \code{"mirt"}).}

\item{engine_version}{Engine package version, as a string.}

\item{data_description}{List describing the data: at minimum \code{source}
(\code{"simulation"} or \code{"matrix"}), plus generator metadata when simulated.}

\item{seed}{The generating/fitting seed (\code{NA} when unknown, e.g. for a
raw matrix of unknown provenance).}

\item{converged}{Logical scalar from the engine's convergence diagnostic.}

\item{log_likelihood}{Fitted log-likelihood. Must be negative -- item
response likelihoods are products of probabilities.}

\item{n_parameters}{Number of estimated parameters.}

\item{estimates}{Data frame of item-parameter estimates.}

\item{warnings}{Character vector of warnings emitted during fitting.}

\item{runtime_seconds}{Wall-clock fitting time in seconds.}
}
\value{
An object of class \code{irtc_model_result} (schema version 1), with
\code{fitted_at} stamped in UTC.
}
\description{
The model-result schema is the project's WP3 contract: every fit --
baseline or copula -- records its engine, versions, seed, convergence
status, warnings, and timing, so that no nonconverged or unprovenanced
fit can slip silently into a model comparison.
}
\seealso{
Other model results:
\code{\link[=model_result_from_json]{model_result_from_json()}},
\code{\link[=model_result_to_json]{model_result_to_json()}}
}
\concept{model results}
