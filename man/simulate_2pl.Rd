% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_2pl}
\alias{simulate_2pl}
\title{Simulate binary responses from a two-parameter logistic (2PL) model}
\usage{
simulate_2pl(n_persons, n_items, seed)
}
\arguments{
\item{n_persons}{Number of persons (rows). Must be at least 1.}

\item{n_items}{Number of items (columns). Must be at least 2.}

\item{seed}{Integer seed. Required -- unseeded simulations are not
reproducible and the charter forbids them in registered experiments.}
}
\value{
An object of class \code{irtc_simulation}: a list with \code{responses}
(integer matrix, persons x items), \code{theta} (numeric vector),
\code{item_parameters} (data frame with \code{item}, \code{a}, \code{b}), and \code{generator}
(list of model, seed, and dimensions).
}
\description{
Generates person abilities \code{theta ~ N(0, 1)}, item discriminations
\code{a ~ Lognormal(0, 0.3)}, and item difficulties \code{b ~ Uniform(-2.5, 2.5)},
then draws Bernoulli responses under
\code{P(Y = 1 | theta) = plogis(a * (theta - b))}.
}
\details{
Every simulation records its generator metadata (model, seed, dimensions)
so downstream model results can carry full provenance.
}
